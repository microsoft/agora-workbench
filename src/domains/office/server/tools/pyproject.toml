[project]
name = "office-tools"
version = "0.1.0"
description = "Office document extraction tools for AgoraAgentMAF code execution server"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
