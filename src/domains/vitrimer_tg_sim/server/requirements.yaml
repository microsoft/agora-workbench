# Vitrimer Tg simulation dependencies (installed in isolated uv kernel inside Docker)

# Local tools package
/app/domains/vitrimer_tg_sim/server/tools

# Cheminformatics (for SMILES validation)
rdkit>=2023.3.1

# Scientific computing
numpy>=1.26.0,<2.0.0
pandas>=2.2.2,<3.0
scipy>=1.11.3

# Piecewise linear fitting for bilinear Tg regression
pwlf>=2.0.0

# EMC wrapper for initial box construction (provides the 'pyemc' import)
emc-pypi>=2025.8.21

# Jupyter kernel (required for code execution)
ipykernel>=6.29.0
