[project]
name = "example-tools"
version = "0.1.0"
description = "Example tools for AgoraAgentMAF code execution server"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
